// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://yunqilin2023-ship-it.github.io',
  base: '/tw-supply-chain/',
  output: 'static',
});
